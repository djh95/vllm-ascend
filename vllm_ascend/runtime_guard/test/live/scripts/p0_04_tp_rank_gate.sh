#!/usr/bin/env bash
# P0-4 stub: TP>=2 rank gate — fill lab TP launch; check only TP0 detects.
source "$(cd "$(dirname "$0")" && pwd)/_common.sh"
echo "[p0_04] TODO: launch TP>=2 with product build; enable one detector; assert non-TP0 silent"
echo "[p0_04] RUNNER=$RUNNER"
